steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args: ['functions', 'deploy', 'c-build-test', '--region', 'us-central1', '--entry-point', 'main', '--runtime', 'python39', '--trigger-http']

options:
  logging: CLOUD_LOGGING_ONLY
