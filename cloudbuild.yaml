steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args: ['functions', 'deploy', 'c-build-test', '--region', 'us-central1', '--entry-point', 'main', '--runtime', 'python39', '--trigger-http']
- name: 'gcr.io/cloud-builders/gcloud'
  args: ['functions', 'add-iam-policy-binding', 'c-build-test', '--region', 'us-central1', '--member=allUsers', '--role=roles/cloudfunctions.invoker']


options:
  logging: CLOUD_LOGGING_ONLY
